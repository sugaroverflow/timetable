# apps/web/public must exist: the web Dockerfile COPYs it next to the
# standalone server. Removing the last real asset (domain cutover, #95)
# deleted the directory from git and broke every Deploy Dev build.
