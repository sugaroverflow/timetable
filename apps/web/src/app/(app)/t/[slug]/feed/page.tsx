export default function FeedPage() {
  return (
    <div className="notice">
      Topic feed — hearts, threaded comments, host-only panels, and moderation —
      lands in Phase 1.
    </div>
  );
}
