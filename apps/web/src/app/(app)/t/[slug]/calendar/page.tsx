export default function CalendarPage() {
  return (
    <div className="notice">
      Availability calendar — timeslots, red/yellow/green availability, and host
      audience filters — lands in Phase 3.
    </div>
  );
}
