export default function ModerationPage() {
  return (
    <div className="notice">
      Moderation queue — publish / request changes / reject, hide comments, and
      the activity timeline — lands in Phase 1.
    </div>
  );
}
